cc {
  set { _
    iwlog_test1.c
  }
  set { _ ..${CFLAGS} -DIW_STATIC }
}

foreach {
  OBJ
  ${CC_OBJS}
  run {
    exec { ${CC} ${OBJ} ${LDFLAGS_TEST} -o %{${OBJ}} }
    consumes { ${LIBIOWOW_A} ${OBJ} }
    produces { %{${OBJ}} }
  }
}

if { defined { RUN_TESTS }
  foreach {
    OBJ
    ${CC_OBJS}
    run {
      always
      shell { %{${OBJ}} }
      consumes { %{${OBJ}} }
    }
  }
}
